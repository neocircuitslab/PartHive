# PartHive has NO third-party pip dependencies.
#
# The EasyEDA->KiCad converter (`easyeda2kicad_ph`) is BUNDLED as a git submodule
# under `easyeda2kicad-ph/` — NeoCircuitsLab's AGPL-3.0 fork of
# uPesy/easyeda2kicad.py — and shipped inside the plugin package. It is NOT
# installed from PyPI. Source: https://github.com/neocircuitslab/easyeda2kicad-ph
#
# Everything else PartHive needs is the Python standard library or provided by
# KiCad itself (wxPython, and on the SWIG path, pcbnew).
